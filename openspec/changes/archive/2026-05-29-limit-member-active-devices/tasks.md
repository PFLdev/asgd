## 1. 配置与契约

- [x] 1.1 新增会员活跃设备限制配置项，包括启用开关、最大活跃设备数、租约 TTL、Redis 异常降级策略
- [x] 1.2 定义会员设备准入请求、结果和状态枚举，覆盖允许、超限、临时不可用、降级放行等结果
- [x] 1.3 明确受限制的会员功能入口，并设计统一接入方式，避免每个 Controller 重复实现判断逻辑

## 2. Redis 租约准入

- [x] 2.1 实现设备标识 hash 工具，确保 Redis Key、审计记录和日志不直接保存原始 `deviceId`
- [x] 2.2 实现 Redis ZSET Key 生成和设备详情 Key 生成，按 `userId` 隔离活跃设备集合
- [x] 2.3 实现 Lua 脚本，原子完成过期清理、当前设备续租、新设备占用和超限拒绝
- [x] 2.4 实现会员活跃设备准入 Service，封装 Redis 调用、结果转换和异常降级策略
- [x] 2.5 实现租约刷新逻辑，确保已占用名额的设备重复请求会员功能时不额外占用名额

## 3. 会员功能接入

- [x] 3.1 在会员功能入口接入准入 Service，从认证上下文或请求中获取 `userId`、`deviceId`、`sessionId`
- [x] 3.2 对超限请求返回稳定业务状态和可解释提示，不继续执行后续会员业务逻辑
- [x] 3.3 确保多设备登录行为不被限制，仅在访问会员功能时执行活跃设备数校验
- [x] 3.4 为 Redis 异常 fail-open 和 fail-closed 两种配置路径接入对应响应行为

## 4. 审计与可观测性

- [x] 4.1 记录设备租约创建、刷新、超限拒绝、释放或过期、Redis 异常等关键事件
- [x] 4.2 增加必要日志字段，包括 `userId`、脱敏设备标识、事件类型、准入结果和发生时间
- [x] 4.3 增加监控指标或预留埋点，覆盖超限拒绝次数、Redis 异常次数、降级放行次数

## 5. 测试验证

- [x] 5.1 添加 Service 测试：当前设备重复请求时刷新租约并放行
- [x] 5.2 添加 Service 测试：少于 3 台活跃设备时新设备可占用名额
- [x] 5.3 添加 Service 测试：已有 3 台活跃设备时第 4 台设备被拒绝
- [x] 5.4 添加并发测试：已有 2 台活跃设备时，多台新设备并发请求最多只有 1 台成功
- [x] 5.5 添加租约过期测试：过期设备不再计入活跃设备数
- [x] 5.6 添加 Redis 异常测试：fail-open 放行并记录事件，fail-closed 拒绝并返回临时不可用
- [x] 5.7 添加入口层测试：会员功能受限制，非会员或登录行为不受该限制影响
- [x] 5.8 运行 `mvn test` 验证现有功能和新增测试通过
